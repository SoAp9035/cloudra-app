# Weather Map – Simple Doc

 
  - [ ] UPPERCASE = ==constant values.==
  - [ ] camelCase = ==variables or functions.==
  

## Step 1 — Show Google Map in React
- Use **Google Maps API** with `<Map />`
- google map key in the **.env** !!
- Important props and defaults:
  - `id="main-map"`
  - `defaultCenter={DEFAULT_CENTER}`
  - `defaultZoom={12}`
  - `gestureHandling="greedy"`
  - `style={{ width: "100%", height: "100vh" }}`
  - ``

---
